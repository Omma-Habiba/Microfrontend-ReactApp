import React from 'react';

const TitleHost = ({ title }) => {
    return (
        <div>
            <h2>{title}</h2>
            <p>Titre unique dans host</p>
        </div>
    );
};

export default TitleHost;
