import React from 'react';

const TitleRemote = ({ title }) => {
    return (
        <div>
            <h2>{title}</h2>
            <p>Titre unique dans remote</p>
        </div>
    );
};

export default TitleRemote;
